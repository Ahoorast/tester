#include<bits/stdc++.h>
using namespace std;

int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int n;
	cin >> n;
	if (n % 2 == 0) 
		for (int i = 0; i < 1e9; i++)
			;
	else
		cout << "LOL\n";
	return 0;
}
